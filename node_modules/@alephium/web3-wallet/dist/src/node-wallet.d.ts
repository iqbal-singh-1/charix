import { Account, SignerProviderWithMultipleAccounts, NodeProvider, ExplorerProvider, Address } from '@alephium/web3';
export declare class NodeWallet extends SignerProviderWithMultipleAccounts {
    walletName: string;
    readonly nodeProvider: NodeProvider;
    readonly explorerProvider: ExplorerProvider | undefined;
    constructor(walletName: string, nodeProvider?: NodeProvider, explorerProvider?: ExplorerProvider);
    setSelectedAccount(address: Address): Promise<void>;
    getAccounts(): Promise<Account[]>;
    protected unsafeGetSelectedAccount(): Promise<Account>;
    signRaw(signerAddress: string, hexString: string): Promise<string>;
    unlock(password: string): Promise<void>;
    lock(): Promise<void>;
}
