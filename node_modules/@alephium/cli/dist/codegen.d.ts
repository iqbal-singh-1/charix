import { Configuration } from './types';
import { Project } from './project';
export declare function genLoadDeployments(config: Configuration): Promise<void>;
export declare function sortByName<T extends {
    artifact: {
        name: string;
    };
}>(artifacts: T[]): T[];
export declare function codegen(project: Project): void;
